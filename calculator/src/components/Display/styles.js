import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    display: {
        flex: 1,
        alignItems: "flex-end"
    },
    displayText: {
        fontSize: 100,
        color: "#000",
        margin: 30
    },
});

export default styles;